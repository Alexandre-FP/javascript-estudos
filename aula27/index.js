// opração ternaria condição (? = ture) , (: = false)

const pontuacaoUsuario = 10;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usiario VIP' : 'Usuario normal';

console.log(nivelUsuario);

const num = 20;
const num2 = 60;
const resultado = num / num2;

console.log(resultado);
