/* 
Operadores de comparação
> maior que
< menor que
>= maior ou igual que
<= menor ou igual que
== igualde (nao é recomendavel utilizar)
=== igualdade restrita (valor e tipo)
!= diferente (nao é recomendavel utiliza)
!==  diferente estrito (valor e tipo)
*/

const num1 = 10;
const num2 = 10;
const soma = num1 !== num2;
console.log(soma)